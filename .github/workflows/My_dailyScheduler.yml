name: Daily Scheduler

on: 
  push:
    branches: "main"
    
jobs: 
  build-and-push:
    runs-on: ubuntu-latest
    
    steps:
      - name: set up python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install python
        run: echo "Hello, Welcome to Daily Scheduler"
